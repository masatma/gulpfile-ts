# Overview

## Using multiple typescript classes
Multiple typescript classes are used to build multiple gulp tasks.
`index.ts` construct all relationships.

# Setup
`> npm install -g gulp`
`> npm install -g typescript`
`> npm install`

# Actions
`> gulp --tasks`
`> gulp build`
`> gulp clean`
