# Overview

## Index.ts only sample
It uses single `index.ts` file to run all gulp tasks. Gulp tasks are defined by gulp.task() function.

# Setup
`> npm install -g gulp`
`> npm install -g typescript`
`> npm install`

# Actions
`> gulp --tasks`
`> gulp build`
`> gulp clean`